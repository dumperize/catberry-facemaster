<div block="next-prev-master" style="{{#unless next}}margin-right: -8px;{{/unless}}">
    {{#if prev}}
        <a elem="prev" href="/{{prev.publication.number}}">
            <!-- Важно! Прослойка между <a> и <svg>. -->
            {{icon "arrLeft"}}
            <span>{{{prev.name}}}</span>
            <img src="{{imgSrc prev.imgID 50 50}}" alt="">
        </a>
    {{/if}}
    {{#if next}}
        <a elem="next" href="/{{next.publication.number}}">
            <img src="{{imgSrc next.imgID 50 50}}" alt="">
            <span>{{{next.name}}}</span>{{icon "arrRight"}}
        </a>
    {{/if}}
</div>